export default function HeroCardImages(props) {
    return (
        <div className="w-full lg:w-1/2 flex flex-col items-center lg:items-end p-8">
        {/* <img src={props.heroImageOne} alt={props.heroImageOneAlt} className="mb-4" />
        <img src={props.heroImageTwo} alt={props.heroImageTwoAlt} /> */}
    </div>
    )
}